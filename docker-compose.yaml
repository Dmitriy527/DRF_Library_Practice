services:
  web:
    build: .
    container_name: django_app
    ports:
      - "8001:8000"
    volumes:
      - ./:/app
    command: >
      sh -c "python manage.py migrate &&
             python manage.py runserver localhost:8000"
